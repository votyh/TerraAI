# TerraAI FastAPI Backend — Python dependencies
# Install: pip install -r requirements.txt
# Run:     uvicorn main:app --reload --port 8000

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic[email]==2.9.2
python-dotenv==1.0.1

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9
# Optional: Redis-backed rate-limit store (more reliable in multi-worker deployments)
# redis==5.2.0

# ── Database — ORM + migrations ──────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0            # async PostgreSQL driver (used with SQLAlchemy async)
psycopg2-binary==2.9.10    # sync fallback / Alembic migrations

# ── Geospatial ────────────────────────────────────────────────────────────────
geoalchemy2==0.15.2        # PostGIS geometry column types for SQLAlchemy
shapely==2.0.6             # Geometry manipulation & WKT/WKB serialisation

# ── HTTP client (async GIS API calls) ─────────────────────────────────────────
httpx==0.27.2

# ── Auth (Supabase JWT verification) ──────────────────────────────────────────
PyJWT==2.9.0

# ── Payments (Stripe Checkout + webhooks) ─────────────────────────────────────
stripe==11.1.0

# ── AI / LLM (Phase 2+) ──────────────────────────────────────────────────────
# google-generativeai==0.8.3   # Gemini 1.5 Pro — activate in Phase 2
# openai==1.51.0               # OpenAI GPT fallback — activate if needed
