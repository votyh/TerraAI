import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remote image domains — add LINZ / Google Maps tile hosts here in Phase 2
  images: {
    remotePatterns: [],
  },
};

export default nextConfig;
